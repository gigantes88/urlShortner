<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/static/index.css">
  <title>URL 짧게 만들기</title>
</head>
<body>
  <h1>URL Shortly</h1>
  <table>
  <thead>
    <tr>
      <tr>
        <td>Long URL</td>
        <td>Short URL</td>
      </tr>
    </tr>
  </thead>
  <tbody>
    <% data.forEach( item => { %>
      <tr>
        <td><%= item.longUrl %></td>
        <td><a href="/<%= item.id %>" target="_blank"><%= item.id %></a></td>
      </tr>
    <% }) %>
  </tbody>
  </table>
</body>
</html>